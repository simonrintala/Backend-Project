package com.Java24GroupProject.AirBnBPlatform.services.BookingMethods;

/************************
 * BookingMethod
 * ---
 * Interface for BookingMethodTemplates. Used as a connection point between the abstract
 * method template classes and the BookingMethodService class.
 * -
 * Could be further developed with a setVariables() and runMethod() methods, however, this
 * might require splitting it into two or more interfaces, to accommodate different input variables
 * used by the CreateUpdateBookingTemplate runMethod() and the AcceptRejectDeleteBookingTemplate runMethod()
 * **********************/

public interface IBookingMethod {
}
